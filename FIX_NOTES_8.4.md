# KeePass Studio Electron 8.4 修复说明

- 数据预览表第一列表头改为“选择”。
- 第二列表头改为“序号”。
- 序号不再显示 Google Sheets 的实际行号；当前排序和筛选结果按 1、2、3……连续编号。
- 翻到第二页时继续显示 11、12……，保证整份预览的序号连续。
- 选择列和序号列的表头、内容均居中。
- Google Sheets 的原始 `sheetRow` 仍保留在内部，用于选择状态、日志回写及数据定位，不展示给用户。
